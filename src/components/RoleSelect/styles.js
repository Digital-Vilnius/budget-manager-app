import styled from 'styled-components';
import { COLORS } from 'styles';

const ListFooter = styled.View`
  height: 30px;
  border-top-width: 1px;
  border-top-color: ${COLORS.LIGHT_GREY};
`;

const Footer = styled.View`
  padding: 10px;
`;

export { ListFooter, Footer };
