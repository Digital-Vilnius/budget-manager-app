import styled from 'styled-components';
import { Ionicons } from '@expo/vector-icons';

const Container = styled.TouchableOpacity``;

const IoniconsIcon = styled(Ionicons)`
  padding-top: 2px;
`;

export { Container, IoniconsIcon };
