import styled from 'styled-components';

const Container = styled.View`
  flex: 1;
`;

const List = styled.SectionList``;

const Separator = styled.View`
  height: 8px;
`;

const Footer = styled.View`
  height: 30px;
`;

export { Container, List, Separator, Footer };
